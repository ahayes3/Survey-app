<server description="SurveyMe Front End">
    <featureManager>
        <feature>servlet-4.0</feature>
    </featureManager>

    <httpEndpoint httpPort="${default.http.port}" httpsPort="${default.https.port}" id="defaultHttpEndpoint"  host="*" />

    <webApplication id="SurveyMeWebApp" location="SurveyMeWebApp.war" contextRoot="/" />
</server>
